# Task 1. Вторая часть не однозначная, указано что нужно прочитать содержимое всех файлов (без папок значит) и не указано про вложенность

echo > file1.txt
cat /etc/* 2> errors1.txt # но он будет пытаться и папки, как сказать чтобы он прочитал только содержимое файлов не нашел параметров
find /etc -maxdepth 1 -type f -name \*.* 2> errors1.txt | cat 2>> errors1.txt # вот если читать только файлы, строго в /etc, но и ошибок тогда не будет

# Task 2. Также не однозначная задача, нужен вывод длинного списка каталога, а потом отобразить только права доступа к файлам - откуда файлы, если список из каталогов...

ls -d -l /etc/* | cut -c 1-11 | sort | uniq # вывел только уникальные права всех каталогов в папке etc

# Task 3. Оказалось что на прошлом задании, я удалил себя из root, смог восстановить черещ GNU

sudo sed -i "s|#PasswordAuthentication yes|PasswordAuthentication no|" /etc/ssh/sshd_config
sudo systemctl restart sshd
sudo sed -i "s|PasswordAuthentication no|#PasswordAuthentication yes|" /etc/ssh/sshd_config
sudo systemctl reload sshd

# restart - перезапускает процесс (новый PID), reload - перечитывает заново файл конфигурации (старый PID)

cat > file2.txt # ввел несколько строк, завершил Ctrl + D

# Опять неоднозначный вопрос. "Создайте файл при помощи команды cat > file_name, напишите текст и завершите комбинацией ctrl+d. Какой сигнал передадим процессу?" - какому процессу, мы ничего не завершаем в этом месте... cat завершается сам, наверное по сигналу 15

mc # Нажимаем Ctrl + O - для возврата в терминал не закрывая mc
ps -C mc | grep 'mc' | cut -c 1-4 # так узнаем PID mc, хотелось бы передать сразу KILL - но ps - возвращает пробелы вначале
pkill -9 mc #или kill -9 PID, который узнали выше
